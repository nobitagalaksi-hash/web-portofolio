#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../json5/lib/cli.js" "$@"
else
  exec node  "$basedir/../../../../json5/lib/cli.js" "$@"
fi
# cmd-shim-target=C:/Users/nobit/OneDrive/Dokumen/KULIAH S1 SISTEM INFORMASI UB/SEMESTER2/DAMP/Web Portfolio UI Design/node_modules/.pnpm/@babel+core@7.29.7/node_modules/json5/lib/cli.js
